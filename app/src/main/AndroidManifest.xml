<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unionpay.numberpassword">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NumberPassword.NumberPassword" />
        <activity android:name=".RememberNumbers.RememberNumbers_main" />
        <activity android:name=".RememberNumbers.RememberNumbers1" />
        <activity android:name=".RememberPoker2.RememberPoker"/>
        <activity android:name=".RememberPoker.RememberPoker"/>

    </application>

</manifest>